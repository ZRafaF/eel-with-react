{
  "scripts": {
    "start": "cd web && npm run start",
    "startBoth": "cd web && npm run build && cd .. && python main.py",
    "startEel": "python main.py",
    "build": "cd web && npm run build",
    "buildBin": "cd web && npm run build && cd .. && python -m eel main.py web/build --onefile --name EelApp",
    "installDep": "npm install && cd web && npm install"
  }
}
